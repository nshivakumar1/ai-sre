route:
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
  receiver: 'alert-router-webhook'

receivers:
  - name: 'alert-router-webhook'
    webhook_configs:
      - url: 'http://alert-router:8000/webhook'
        send_resolved: true
